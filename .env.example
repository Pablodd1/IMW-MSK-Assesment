# This project uses Cloudflare Workers bindings (D1, R2).
# Traditional .env files are not used for these resources in the current architecture.
#
# If migrating to Vercel/Node.js, you would need to replace D1 bindings with standard DB connections:
# DATABASE_URL="postgresql://user:password@host:port/dbname"
#
# And for Object Storage (R2):
# R2_ACCESS_KEY_ID="your_access_key"
# R2_SECRET_ACCESS_KEY="your_secret_key"
# R2_BUCKET_NAME="webapp-videos"
# R2_ENDPOINT="https://<accountid>.r2.cloudflarestorage.com"
